# CLAUDE.md — zenkaku_normalize

## 作業対象
- `ses_work/line_webhook/line_query.py` の `detail_query` 関数を修正する

## 禁止事項
- `line_query.py` 以外のファイルを変更しない
- 既存の関数シグネチャを変更しない
- `_NUM_MAP` の既存エントリを削除しない
- テストファイルを作業ディレクトリ外に置かない

## 完了条件
- TASKS.md の全チェックボックスが ✅ になること
- `line_query.py` がシンタックスエラーなしで import できること
