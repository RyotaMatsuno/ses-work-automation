{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "gunicorn webhook_server:app --bind 0.0.0.0:8000 --workers 1 --timeout 60"
  }
}
