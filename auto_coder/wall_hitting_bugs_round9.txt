=== round9 by gpt-5.4 ===
in=1395 out=2310 cost=$0.0248

以下で確定でよいです。

## 結論
**今回は「過去合意済みC案の実行」に該当。自走OK。**
理由:
- 新機能追加ではない
- 運用ルール変更ではなく**汚染データの是正**
- 精度を下げる変更ではなく、**誤新着を止めるための回復措置**

---

## Q1. SBT 6件の処理判断
**結論: 全件そのまま「終了」化でよい。1件ずつ本文Date確認は不要。**

理由:
- 既に前チャットで**類似度0.85以上 = 汚染確定**で合意済み
- 6件すべて **2026-06-15 created** で、今回のdedup破損タイミングと一致
- 業務リスクは「古い案件を新着として流し続けること」の方が大きい
- 今回は**再開案件の救済**ではなく**誤登録除去**が目的

**処理対象確定:**
- Prisma Access設計構築
- 建設業向けのERPパッケージ導入(会計領域)
- テスト、ヘルプデスク、運用保守、データ移行@南行徳
- AWS案件/運用ツール構築/運用自動化@五反田
- AWS案件/運用ツール構築/運用自動化
- 置局サポート業務

---

## Q2. 国保系2件・信販系2件の自己重複処理
**結論: (c)でも(d)でもなく、まずは本文確認して判定。**
つまり実務判断は **「一律終了しない。2ペアは案件詳細を見て送信者・Dateで確定」**。

### 理由
この4件はSBT 6件と違って、
- 類似は高いが
- **タイトル差分が募集更新の可能性を持つ**
  - `(8月)` は募集時期更新かもしれない
  - `(半年短期予定)` は条件追加かもしれない

なので、ここだけは**自動終了NG**。

### 判定基準
**同一送信者 × 同一本文骨子 × 片方が過去日付**なら、古い方/再登録側を終了。

見るべき順:
1. 案件詳細内の **メールDate**
2. **送信者**
3. 本文の差分
   - 稼働開始月
   - 期間
   - 単価
   - 場所
   - 面談回数
   - 人数
4. どちらがより後着か

### 実務ルール
- **同一案件の軽微表記差分だけ** → 古い再登録側を終了
- **募集条件更新がある** → 新しい方を残す、旧版を終了
- **判定不能** → 一旦保留、終了しない

### 現時点の暫定推奨
- **国保系2件**: 本文確認必須
- **信販系2件**: 本文確認必須

**この4件は今ここで断定終了しない。**

---

## Q3. matching_v3への影響
**結論: まずDB修正を先に実施。その後、通知有無を確認。**

### リカバリ手順
1. **対象10件のうち確定分6件を先に終了化**
2. matching_v3の送信ログ確認
   - 対象page_id
   - タイトル
   - 送信時刻
   - 宛先
3. 既送信があれば、必要なら取消/訂正連絡
   - ただし通常は**案件終了済み扱いで後続停止**で十分
   - 所属への訂正メールは**実害が出ている場合のみ**

### 確認すべきもの
**優先度高**
- matching_v3実行ログ
- mail送信ログ
- page_idベースの通知履歴

**優先度中**
- `cost_log.jsonl` → 実行痕跡確認には使えるが、一次ソースではない
- Notion AI作業キュー → キュー投入有無の確認には有用だが、送信事実確認は別

### 断定
- **送信済み確認のために `cost_log.jsonl` は必須ではない**
- **最優先は matching_v3 / mail送信ログ**

---

## Q4. 終了化のNotion API操作
Notionの`ステータス`が **status型** か **select型** かで body が違います。

### A. `ステータス` が status型の場合
```json
{
  "properties": {
    "ステータス": {
      "status": {
        "name": "終了"
      }
    }
  }
}
```

### B. `ステータス` が select型の場合
```json
{
  "properties": {
    "ステータス": {
      "select": {
        "name": "終了"
      }
    }
  }
}
```

### PATCH例
```http
PATCH https://api.notion.com/v1/pages/{page_id}
Authorization: Bearer {NOTION_API_KEY}
Notion-Version: 2022-06-28
Content-Type: application/json
```

body(status型):
```json
{
  "properties": {
    "ステータス": {
      "status": {
        "name": "終了"
      }
    }
  }
}
```

body(select型):
```json
{
  "properties": {
    "ステータス": {
      "select": {
        "name": "終了"
      }
    }
  }
}
```

---

## Q5. 終了化と同時に「終了理由」を記録すべきか
**結論: 記録すべき。最小でも案件詳細末尾に追記。**

推奨順:
1. **専用プロパティがあるならそこに記録**
2. なければ **案件詳細末尾に追記**
3. ステータス変更のみ、は非推奨

### 推奨追記文
```text
[運用メモ 2026-06-18]
2026-06-14〜15のmail_pipeline dedup破損により過去終了案件が新着再登録された汚染データと判定。再募集ではないため終了化。
```

理由:
- 後から見て「なぜ終了化したか」が残る
- 再調査時に迷わない
- 人手レビューの監査証跡になる

---

## Q6. 実行順序の推奨
**結論: 一括ではなく、2段階。**

### 推奨順序
1. **SBT確定汚染6件を先に終了化**
2. **matching_v3送信有無を確認**
3. **国保2件・信販2件を本文確認**
4. 判定できたものだけ終了化
5. 判定不能は保留

### 理由
- 副作用最小
- 確定汚染を即止血できる
- グレー案件を誤って潰さない

**つまり「6件は即実行、4件は確認後」**

---

## Q7. ジョブズ自走範囲の判断
**結論: 自走OK。松野確認は不要。**

理由:
- 既に**C案合意済み**
- 今回はその実行
- 「使い方変更」「料金増」「モデル変更」「品質閾値変更」ではない
- むしろ誤新着抑止のための**是正措置**

ただし、
- **国保/信販の4件で本文読んでも判定不能**
- **訂正メールを所属に送る必要が出た**
この2点だけは、必要なら松野確認でよいです。

---

# 最終実行方針
## 今すぐやる
- **SBT 6件を全件「終了」化**
- 理由を記録
- matching_v3送信有無を確認

## その後やる
- **国保2件・信販2件は本文Date/送信者確認**
- 判定できたものだけ終了化

---

# そのまま使うPATCH body例

## status型
```json
{
  "properties": {
    "ステータス": {
      "status": {
        "name": "終了"
      }
    }
  }
}
```

## select型
```json
{
  "properties": {
    "ステータス": {
      "select": {
        "name": "終了"
      }
    }
  }
}
```

## 理由メモも同時更新する例（案件詳細がrich_text想定）
```json
{
  "properties": {
    "ステータス": {
      "status": {
        "name": "終了"
      }
    },
    "案件詳細": {
      "rich_text": [
        {
          "type": "text",
          "text": {
            "content": "既存本文...\n\n[運用メモ 2026-06-18]\n2026-06-14〜15のmail_pipeline dedup破損により過去終了案件が新着再登録された汚染データと判定。再募集ではないため終了化。"
          }
        }
      ]
    }
  }
}
```

※ `案件詳細` を上書きする実装なら注意。既存本文を取得して末尾追記してからPATCH。

必要なら次に、**10件を「即終了6件 / 要確認4件」に分けた実行用一覧**の形で出します。