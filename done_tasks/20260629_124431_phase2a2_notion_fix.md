# Phase 2A2 修正: Notion正規化スキルプロパティ追加

## 背景
- normalize_all_skills.py --apply で全189件が失敗
- 原因: エンジニアDBに「正規化スキル」プロパティが存在しない
- Notion API: "正規化スキル is not a property that exists."

## 対応方針（2択）

### 案A: 正規化スキルプロパティをNotionに追加（推奨）
1. エンジニアDB (343450ff-37c0-819d-8769-fb0a8a4ceeb1) に
   「正規化スキル」multi_selectプロパティを追加
2. normalize_all_skills.py の更新ロジックがmulti_selectに対応しているか確認
3. --apply 再実行

### 案B: 既存「スキル」プロパティを直接更新
1. normalize_all_skills.py で「スキル」プロパティを上書き更新
2. 元のスキル情報が失われるリスクあり → 非推奨

## 参照ファイル
- matching_v3/scripts/normalize_all_skills.py
- matching_v3/matcher.py L361: skills = engineer.get("正規化スキル") or engineer.get("スキル") or []
- matching_v3/SPEC_phase2a2.md

## 完了条件
- normalize_all_skills.py --apply が正常完了
- matcher.py が正規化スキルを正しく読み取ること
- 既存テスト全PASS

## 優先度
低（matcher.pyは実行時にskill_aliases.jsonで正規化するため、Notion永続化はnice-to-have）
