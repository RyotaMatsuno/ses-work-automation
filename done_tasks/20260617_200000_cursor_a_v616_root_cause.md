# Cursor作業指示 a: 6/16 119行差分の解析 + feature flag 隔離(根本原因究明)

最終更新: 2026-06-17 20:00 (ジョブズ作成)
GPT-5.4 round1/round2 壁打ち合意済み(auto_coder/wall_hitting_bugs_round1.txt / round2.txt)

## 背景

2026-06-16 11:54 に mail_pipeline.py に行われたコード修正(67567→65094 bytes / +119行)が、
2026-06-17 6/13-17 にかけての案件登録停止(Notion 500 Internal Server Error)を誘発した疑い。

ジョブズの予備調査で以下の差分を検出済み:

### v5.1 (6/4版・現本番) が参照するNotionプロパティ
- ステータス (select)
- 備考(LINEメモ) (rich_text)
- **名前 (title)** ← engineer DBのtitleフィールド(必須)
- 案件名 (title)
- 案件詳細 (rich_text)
- 稼働状況 (select)

### v6.16 (壊れた版) が参照するNotionプロパティ
- ステータス (select)
- 案件名 (title)
- 案件詳細 (rich_text)
- 稼働状況 (select)

### 重要な観測
**v6.16 では `名前` (title型・必須) と `備考(LINEメモ)` の参照が消えていた**

→ engineer DBへの page create 時に title が欠落していた可能性
→ これが Notion 500 を誘発した有力候補

## 対象ファイル

- 解析対象: `mail_pipeline/mail_pipeline.py.bak_emergency_20260617_182923` (v6.16壊れた版)
- 比較対象: `mail_pipeline/mail_pipeline.py.bak_phase4` (v5.1正常版・現本番)
- 出力先: `mail_pipeline/v616_root_cause_analysis.md`

## 作業内容

### Phase 1: 差分の完全可視化
1. `diff -u mail_pipeline.py.bak_phase4 mail_pipeline.py.bak_emergency_20260617_182923 > diff_v51_vs_v616.txt`
2. 差分の119行を全て分類:
   - 追加行(+)
   - 削除行(-)
   - 変更行
3. 各変更を機能ブロック単位にグルーピング(例: title生成箇所、property mapping、API call、エラーハンドリング、dedupe、CostGuard、Batch API)

### Phase 2: Notion API payload 生成箇所の特定
v5.1 と v6.16 の「engineer_db / project_db への page create payload を作る関数」を特定し、
プロパティごとの差分を表形式で出力:

| プロパティ名 | 型 | v5.1での処理 | v6.16での処理 | 差分の影響 |
|---|---|---|---|---|
| 名前 | title | xxx を title array にセット | (欠落) | title欠落→500の可能性 |
| 備考(LINEメモ) | rich_text | (xxx) | (欠落) | 補助情報の喪失 |
| ... | ... | ... | ... | ... |

### Phase 3: 「名前」欠落仮説の検証(最小再現テスト)
GPT-5.4 round2 提案の手順:

1. **ケースA**: `名前` を含む正しい create payload を engineer DB に投げる → 成功するか
2. **ケースB**: `名前` を完全に外した create payload を投げる → 400か500か
3. **ケースC**: `名前` はあるが空 title array → 400か500か
4. **ケースD**: `名前` はあるが型を崩す(rich_text 形式で送る) → 400か500か

これらを `tests/test_notion_engineer_payload.py` として書き、結果を `v616_root_cause_analysis.md` に追記。

**注意**: 実DBで実行するため、テスト後はそのpageを即archive。各テスト後にp.archive=trueでクリーンアップ。

### Phase 4: 他の差分パターン候補もチェック
GPT-5.4 round2 で挙げられた他の候補も検証:

- B. property名はあるが型不一致(rich_text⇔title の取り違え)
- C. select/status の不正値(選択肢追加忘れ・全角半角違い)
- D. relation先 page_id 不正(空文字・UUID崩れ・archive済み)
- E. payload JSON自体の壊れ(None混入・list/dict不整合)
- F. create/update 分岐が壊れた(createすべきところでupdate / DB取り違え)

### Phase 5: Feature Flag 隔離設計
今後同様の障害を防ぐため、Notion API request 直前で以下を実装:

```python
import os
NOTION_FLAG_VALIDATE = os.environ.get("NOTION_FLAG_VALIDATE", "true").lower() == "true"

def validate_engineer_payload(payload):
    """engineer DB create前のpayload検証。デフォルトON。"""
    if not NOTION_FLAG_VALIDATE:
        return True
    props = payload.get("properties", {})
    # 必須title「名前」のチェック
    name_prop = props.get("名前")
    if not name_prop or "title" not in name_prop:
        raise ValueError("Notion payload validation: '名前' title is missing")
    title_arr = name_prop.get("title", [])
    if not title_arr or not title_arr[0].get("text", {}).get("content"):
        raise ValueError("Notion payload validation: '名前' title content is empty")
    return True
```

同様に project_db 用も追加。Notion API request 直前で必ず通す。

### Phase 6: 結論まとめ
`v616_root_cause_analysis.md` に以下を記述:
- 119行差分の機能ブロック分類
- 「名前」欠落仮説の検証結果(成立 or 不成立 or 別原因)
- 真の根本原因の確定
- 今後の予防策(Feature Flag, payload validation, gate_checker review)
- v6.16 の改修方針(ロールバック維持 or 修正再デプロイ)

## 完了条件

- diff_v51_vs_v616.txt (119行差分全分類)
- v616_root_cause_analysis.md (Phase 1-6の全結論)
- tests/test_notion_engineer_payload.py (ケースA-D実行可能、結果記録)
- mail_pipeline.py に payload validation 追加(Feature Flag付き)
- 動作確認: 19:00 Cron 1回成功後にデプロイ・1日見守り

## ゲート

- ゲート①(設計レビュー): SPEC作成後 GPT-5.4 で `python gate_checker/gate_check.py --phase design --file v616_root_cause_analysis.md`
- ゲート②(実装レビュー): 実装完了後 GPT-5.4 で `python gate_checker/gate_check.py --phase implementation --file mail_pipeline/mail_pipeline.py`

## 参照ファイル

- ジョブズの予備調査: `auto_coder/schema_diff_v51_vs_notion.py`(プロパティ参照解析)
- GPT-5.4 round1: `auto_coder/wall_hitting_bugs_round1.txt`
- GPT-5.4 round2: `auto_coder/wall_hitting_bugs_round2.txt`
- 引き継ぎ: 2026-06-17_メール基盤緊急復旧
