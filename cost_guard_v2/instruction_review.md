# 指示書文面レビュー (Claude Code 即実装入り評価)

日時: 2026-06-17T12:30:16.964983
model: gpt-5.4
usage: {"prompt_tokens": 1681, "completion_tokens": 1521, "total_tokens": 3202, "prompt_tokens_details": {"cached_tokens": 0, "audio_tokens": 0}, "completion_tokens_details": {"reasoning_tokens": 212, "audio_tokens": 0, "accepted_prediction_tokens": 0, "rejected_prediction_tokens": 0}}

---

【追加修正必要】

- **「最初の応答は文章ではなく、Read/Write などのツール実行から開始する」と明記した方がよいです。**  
  現状でもかなり強いですが、Claude Code は「了解しました。まず参照します」のような短い前置きを出すことがあります。  
  追加推奨文言:
  - `最初の応答は説明文ではなくツール実行とする。自然言語の前置きは禁止。`
  - `3ファイルを読了後、次の応答で即ファイル書き込みを実行する。説明・要約・計画の出力を挟まない。`

- **「Write ツール」という表現を、Claude Code が実際に解釈しやすい表現へ寄せた方がよいです。**  
  Claude Code は文脈上「ファイルを編集・作成する」と理解しますが、必ずしも `Write` という名前のツールを持つとは限りません。ツール名固定で書くより、行動要件で縛る方が安全です。  
  修正文言例:
  - `参照後は直ちに対象ファイルの新規作成または編集を実行する。`
  - `実装開始の合図として、最初の編集操作で Phase 0 のファイルを1つ以上作成する。`

- **「読んだら即実装」をさらに機械的にするため、最初に必ず作る具体的ファイルを1つに絞って指定した方がよいです。**  
  今は `.env`、`dedup_target_required.json`、`migrate_to_sqlite_v2.4.py` と複数候補があり、モデルが一瞬迷う余地があります。  
  追加推奨:
  - `最初の書き込み対象は ses_work/config/dedup_target_required.json とする。これを作成した後、他ファイルへ進む。`
  
- **「承認要求をしない」だけでなく、「計画のみを返して終了した場合は失敗」と明示した方がよいです。**  
  これがあると、モデルが“親切な確認”に逃げる余地をさらに減らせます。  
  追加推奨文言:
  - `計画・確認・質問のみを出力して終了することは禁止。少なくとも1件以上のファイル作成または編集を行わずに終了してはならない。`

- **質問ファイル作成の条件はあるが、「質問前に実装可能部分は先に書く」を足した方がよいです。**  
  そうしないと、軽微な不明点で早期停止する余地が残ります。  
  追加推奨:
  - `不明点があっても、依存しない箇所は先に実装する。質問ファイル作成は、実装継続が不可能な場合に限る。`

- **`.env` 作成指示はややリスクがあります。**  
  仕様上本当に必要ならよいですが、環境ファイル作成は既存運用に影響しやすく、Claude Code が慎重になりやすい箇所でもあります。最初の書き込み対象としては、JSON や migration の方が安全です。  
  見直し推奨:
  - Phase 0 の最初の作成対象から `.env` を外す、または後順に回す。

- **TASKS.md のチェック更新を義務化している点は、停止要因ではないが実装優先度を下げる可能性があります。**  
  先にコード実装、後でまとめてチェック更新の方が、ツール呼び出しの迷いが減ります。  
  修正案:
  - `各 Phase 完了ごと、または関連実装完了後に TASKS.md を更新する。コード実装を優先する。`

- **最後の `IMPL_COMPLETE` 条件は明確ですが、「未完了時は出さない」を加えるとより安全です。**  
  追加推奨:
  - `全実装・必要テスト・TASKS更新が完了した場合のみ IMPL_COMPLETE を出力する。`

### 総評
この指示書は**前回よりかなり改善**されており、特に冒頭の

- 承認要求禁止
- 計画提示不要
- Read後即実装
- 完了時の固定文字列

は有効です。  
ただし、**Claude Code がまだ「まず確認します」「実装方針は以下です」と1ターン使う余地が少し残っています。**  
そのため判定は **【十分】までは届かず、【追加修正必要】** です。

### 特に効く最小追記
以下の3行を冒頭に足すと、かなり止まりにくくなります。

```markdown
- 最初の応答は自然言語の説明ではなく、ツール実行から開始する。
- 3つの必須参照ファイルを読了した次の操作で、必ず1件以上のファイル作成または編集を実行する。
- 計画・確認・質問のみを出力して終了することは禁止。少なくとも1ファイルを変更するまで終了してはならない。
```

必要なら、この指示書を**Claude Code 向けにさらに止まりにくい文面へ全面リライト**できます。
