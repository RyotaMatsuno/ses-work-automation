# ゲート① 設計レビュー (GPT-4o)

### SPEC.mdのレビュー

1. **ロジックの抜け/矛盾**
   - 「送信/請求/契約/本番DB更新を含む依頼」は人間確認が必要とされていますが、具体的な確認フローが記載されていません。確認の手順や責任者を明記する必要があります。

2. **エッジケース漏れ**
   - 「判定が曖昧」の場合の1問確認が無限ループしないことは記載されていますが、具体的な停止条件が不明です。何回確認しても判定できない場合の処理を明確にする必要があります。

3. **CostGuardの被覆**
   - CostGuardの具体的な制限値やどのように制御されるかが不明です。特に、LLMの利用がどのようにCostGuardに影響するかを明確にする必要があります。

4. **危険パラメータの無断増加リスク**
   - FETCH_LIMITやbatch、cron頻度、max_resultsについての具体的な制限や監視方法が記載されていません。これらのパラメータの変更がシステムに与える影響を考慮し、変更時の承認プロセスを設けるべきです。

5. **テスト網羅性**
   - 正常系と異常系のテストケースは基本的なものに限られており、より多くのエッジケースをカバーする必要があります。特に、異常系でのDB操作やネットワーク障害時の挙動をテストするケースが不足しています。

6. **実データ形式の前提ズレ**
   - スキルシートの添付ファイル形式（PDF/Excel）に対する処理が記載されていますが、他の形式（例えばWordや画像ファイル）に対する対応が不明です。これらの形式も考慮すべきです。

7. **LINE push 200通/月制約への対応**
   - 非同期完了通知のpush制限に対する具体的な節約策（ダイジェスト化やreply優先）の実装方法が不明です。これらの動作が確実に行われるようにするための設計が必要です。

8. **Notion本番DB肥大化リスク**
   - 定期失効の具体的なスケジュールや条件が不明です。どのような頻度で行われるのか、また失効時のデータのバックアップやアーカイブ方法も明記する必要があります。

9. **同時更新/重複処理**
   - 同一案件の重複依頼に対する処理が「重複検知 or 別task_idで安全に処理」とされていますが、具体的な重複検知の方法が不明です。重複検知のアルゴリズムや閾値を明確にする必要があります。

10. **ロールバック可能性**
    - 失敗時のblocked状態におけるロールバック方法が記載されていません。失敗時にどのようにシステム状態を戻すのか、具体的な手順を設けるべきです。

### 修正提案
- 送信/請求/契約/本番DB更新を含む依頼の確認フローを明記する。
- 曖昧判定の停止条件を具体化する。
- CostGuardの制限値と制御方法を明確にする。
- 危険パラメータの変更に対する承認プロセスを設ける。
- より多くのエッジケースをテスト計画に追加する。
- スキルシートの他の形式への対応を考慮する。
- LINE push制限に対する具体的な節約策を設計する。
- Notion DBの定期失効のスケジュールと方法を明記する。
- 重複検知のアルゴリズムを明確にする。
- ロールバック手順を設ける。

【判定: 条件付きGO】
